{
  "name": "@hanais/agent-team",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && rm -rf dist/roles dist/skills && cp -R src/roles src/skills dist/",
    "test": "pnpm build && node test/smoke.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
